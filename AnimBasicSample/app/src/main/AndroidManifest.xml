<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="anim.lhx.animbasicsample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlphaAnimActivity" />
        <activity android:name=".FrameAnimActivity" />
        <activity android:name=".ObjectAnimActivity" />
        <activity android:name=".RotateAnimActivity" />
        <activity android:name=".ScaleAnimActivity" />
        <activity android:name=".TranslateAnimActivity" />
        <activity android:name=".ValueAnimActivity" />
        <activity android:name=".ObjectAnimByPathActivity"></activity>
    </application>

</manifest>